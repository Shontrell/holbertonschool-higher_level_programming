#include "lists.h"
/**
 * check_cycle - checks if a singly linked list has a cycle in it
 * @list: linked list that's being checked
 * Return: 0 if no cycle or 1 if cycle
 */
int check_cycle(listint_t *list)
{
	return (0);
}
